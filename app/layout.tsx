import type { ReactNode } from 'react'

// Root layout — the [locale] layout handles html/body/providers
export default function RootLayout({ children }: { children: ReactNode }) {
  return children
}
